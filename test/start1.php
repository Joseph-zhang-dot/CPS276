<?

for ($i=1; $i<=10; $i++){
    echo "<p>I have counted to $i</p>";
    }
/*
$string = "this is a
multi-line string";
echo $string;
$string = 'this is a multi-line string with "quotes" the double quotes render because the rest of the string is wrapped in single quotes';
echo $string;
$string = "this is a multi-line string with \"quotes\" the double quotes render because they are escaped with the backslash character.";
echo $string;
$string = <<<STR
This is a heredoc multi-line string with "quotes", the double quotes render because we are using the heredoc, where both 'single' and "double quotes" render;
STR;
echo $string;
*/

?>